CREATE TABLE autor (
    usr VARCHAR(255),
    paswd VARCHAR(255)
);

CREATE TABLE usr_info (
    usr VARCHAR(255),
    openkey TEXT
);

CREATE TABLE chat (
    logn VARCHAR(255),
    frm VARCHAR(255),
    mess TEXT,
    idd INTEGER
);

